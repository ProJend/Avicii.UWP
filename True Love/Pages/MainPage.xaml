<Page
    x:Class="True_Love.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:True_Love.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Grid x:Name="LayoutRoot">
        <ScrollViewer x:Name="sv" ViewChanged="sv_ViewChanged" VerticalScrollBarVisibility="Hidden">
            <Frame x:Name="ContentFrame" IsTabStop="False"/>
        </ScrollViewer>
        
        <muxc:NavigationView x:Name="NavView"
                        Loaded="NavView_Loaded"
                        BackRequested="NavView_BackRequested"
                        ItemInvoked="NavView_ItemInvoked"
                        PaneDisplayMode="Auto"
                        IsBackButtonVisible="Auto"
                             IsTitleBarAutoPaddingEnabled="False" d:IsLocked="True">
            <muxc:NavigationView.MenuItems>
                <!--<muxc:NavigationViewItem Icon="Setting"/>-->
                <muxc:NavigationViewItemHeader x:Uid="Mnh1" x:Name="MainPagesHeader"/>
                <muxc:NavigationViewItem x:Uid="Mnh2" Icon="Contact" Tag="home"/>
                <muxc:NavigationViewItemSeparator/>
                <muxc:NavigationViewItem x:Uid="Mnh3" Icon="World" Tag="comments"/>
                <muxc:NavigationViewItem x:Uid="Mnh4" Icon="Shop" Tag="images"/>
            </muxc:NavigationView.MenuItems>
        </muxc:NavigationView>

        <Grid x:Name="bar" Canvas.ZIndex="2" VerticalAlignment="Bottom">
            <CommandBar DefaultLabelPosition="Bottom" >
                <CommandBar.Background>
                    <AcrylicBrush TintColor="#000001" TintOpacity="0.3" BackgroundSource="Backdrop"/>
                </CommandBar.Background>
                <!--<AppBarButton Icon="Upload" Label="返回顶部" Style="{StaticResource AppBarButtonRevealStyle}" ToolTipService.ToolTip="返回顶部"/>
                <AppBarButton Icon="Add" Label="分享故事" Style="{StaticResource AppBarButtonRevealStyle}" ToolTipService.ToolTip="分享你与 Avicii 的故事"/>
                <AppBarButton Icon="Refresh" Label="刷新" Style="{StaticResource AppBarButtonRevealStyle}" ToolTipService.ToolTip="刷新数据"/>
                <AppBarButton Icon="Setting" Label="设置" Style="{StaticResource AppBarButtonRevealStyle}" ToolTipService.ToolTip="打开设置"/>
                <AppBarSeparator/>-->
                <AppBarButton Label="Spotify" Style="{StaticResource AppBarButtonRevealStyle}" ToolTipService.ToolTip="Sptify Music" Click="Spotify_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xeaae;" FontFamily="{ThemeResource iconfont}" Margin="-3"/>
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton Label="YouTube" Style="{StaticResource AppBarButtonRevealStyle}" ToolTipService.ToolTip="YouTube MV" Click="YouTube_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xe660;" FontFamily="{ThemeResource iconfont}" Margin="-3"/>
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton Label="Apple Music" Style="{StaticResource AppBarButtonRevealStyle}" ToolTipService.ToolTip="Apple Music" Click="Apple_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xe61a;" FontFamily="{ThemeResource iconfont}" Margin="-3"/>
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton Label="Netease Music" Style="{StaticResource AppBarButtonRevealStyle}" ToolTipService.ToolTip="Netease Music" Click="Netease_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xe628;" FontFamily="{ThemeResource iconfont}" Margin="-3"/>
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton Label="QQ Music" Style="{StaticResource AppBarButtonRevealStyle}" ToolTipService.ToolTip="QQ Music" Click="QQ_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xe6c6;" FontFamily="{ThemeResource iconfont}" Margin="-3"/>
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarSeparator/>
                <AppBarButton Label="Instagram" Style="{StaticResource AppBarButtonRevealStyle}" ToolTipService.ToolTip="Instagram" Click="Instagram_Click">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xe62e;" FontFamily="{ThemeResource iconfont}" Margin="-3"/>
                    </AppBarButton.Icon>
                </AppBarButton>

                <CommandBar.SecondaryCommands >
                    
                </CommandBar.SecondaryCommands>

                <!--<CommandBar.Triggers>
                    <EventTrigger RoutedEvent="Page.Loaded">
                        <BeginStoryboard>
                            <Storyboard x:Name="Storyboard1">
                                <DoubleAnimation Storyboard.TargetName="Translation" Storyboard.TargetProperty="X" From="0" To="200" >
                                    <DoubleAnimation.EasingFunction>
                                        <CircleEase EasingMode="EaseInOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </CommandBar.Triggers>-->
            </CommandBar>
        </Grid>

        <Grid Height="32" VerticalAlignment="Top" HorizontalAlignment="Stretch" x:Name="AppTitleBar" Background="Transparent" d:IsLocked="True" Margin="80,0,0,0"/>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger
                        MinWindowWidth="{x:Bind NavView.CompactModeThresholdWidth}"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!-- Remove the next 3 lines for left-only navigation. -->
                        <Setter Target="NavView.PaneDisplayMode" Value="Top"/>
                        <Setter Target="MainPagesHeader.Visibility" Value="Collapsed"/>
                        <!-- Leave the next line for left-only navigation. -->
                        <Setter Target="AppTitleBar.Margin" Value="390,0,0,0"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
